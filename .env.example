# Admin Authentication
# Generate a secure password hash using: node -e "const crypto = require('crypto'); console.log(crypto.createHash('sha256').update('your-password' + 'your-salt').digest('hex'))"
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-here
ADMIN_PASSWORD_HASH=
ADMIN_SALT=change-this-to-random-string-in-production

# Node Environment
NODE_ENV=production

